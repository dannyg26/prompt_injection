# Evaluation protocol ? supersession notice

The original draft v0.1 is superseded and preserved under `artifacts/superseded_pre_cleanup/docs/PROTOCOL.md`. Neither version is a preregistration.

Use the [two-page v0.3 specification](EXPERIMENT_SPEC.md), [annotation/data register](DATASETS.md), [repair procedure](DATA_REPAIR.md) and [standing rules](../AGENTS.md).

Every performance claim must identify seeds and confidence intervals with their assumptions. Current repair runs intentionally retain the old 5% empirical validation-FPR recipe for comparability; the proposed larger study targets 1%. Do not conflate them. Final hashes, hyperparameters, adequate data and an immutable analysis plan must be frozen before confirmatory evaluation.
