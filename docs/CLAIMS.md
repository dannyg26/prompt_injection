# Claims register

Every finding the paper may state lives here first. The paper may not state a claim more strongly than its row allows.

- **Status** comes from our own results.
- **Novelty** comes from the novelty-auditor, using page-cited literature notes only.

Status values: SUPPORTED, SUPPORTED-DESCRIPTIVE (no inferential test), NULL (no effect detected; the interval is given), WITHDRAWN, KNOWN (standard result, used as a practical note).
Novelty values: UNVERIFIED, ALREADY SHOWN, PARTIALLY SHOWN, NOT FOUND IN N PAPERS READ.

| ID | Claim (the exact scope the paper may use) | Evidence | Status | Novelty |
| --- | --- | --- | --- | --- |
| C1 | (a) Descriptive: in the InjecGuard release, the 5,000 HackAPrompt attack rows are full templated prompts (competition level template plus user input) and form 6 lexical groups, which correspond to the competition's level templates with several levels merged. This concentration follows from the competition's design; any row-level split of this source shares level templates between train and test. (b) Across the release, attack classes are more template-concentrated than benign classes under our lexical grouping (exhaustive counts; per-source sums 3.1 attack vs 1.2 benign rows per group; 2.1 vs 1.2 excluding HackAPrompt; BIPIA benign, 3.0, is an exception). The contrast is extreme only for HackAPrompt. *Scope narrowed 2026-09-25 by the novelty-auditor from "far more template-concentrated … benign sources are about 1 row per group".* | docs/BENCHMARK_AUDIT.md §2; results/audit/benchmark_overlap.json (template_concentration_train, all 28 cells). Level mapping: data check in docs/literature/hackaprompt-2023.md "Our data check" (not yet saved as a result file). Lit: hackaprompt-2023 p. 3 §3.1, p. 4 (fixed template per challenge, input inserted at a placeholder, full prompt fed to the LLM), p. 21 App. E.2 (release stores `prompt` and `user_input` separately; redundancy/spam stated, not quantified), pp. 22–25 App. F (templates); injecguard-2024 p. 6 §4.1, p. 14 Tables 4–5 (per-source counts; no template, dedup or group structure described); bipia-2025 p. 3 Table 1 (crossed design; our inference); malpid-2024 pp. 2–3 (templated attack construction); pidsbench-2026 pp. 7, 11, 17; whenbenchmarkslie-2026 p. 9; capture-2025 p. 2 §2.2 (uses HackAPrompt, no concentration analysis) | SUPPORTED (exhaustive counts) | (a) ALREADY SHOWN by construction: HackAPrompt pp. 3–4, 21, 22–25 document one fixed template per level, and the release's full-prompt field. Ours is only the observation that InjecGuard used the full-prompt field (our data check; InjecGuard pp. 6, 14 do not say which field) and the consequence for row splits. Report as a descriptive data-release caveat, citing HackAPrompt, not as a finding. (b) PARTIALLY SHOWN: templated attack construction is documented source by source (HackAPrompt p. 3; BIPIA p. 3; MalPID pp. 2–3; PIDS-Bench p. 7), so "attack data are templated" is inferable. Not found in the 14 papers read: a cross-source measurement of attack-vs-benign concentration in a merged release (InjecGuard pp. 6, 14 describe none). Outside HackAPrompt the asymmetry is modest; do not use "far more". Grouping validity: on HackAPrompt, where templates are known by construction, no known template was split across lexical groups (3 stray rows), but 5 of 11 templates were chained into one group (7 groups in the HackAPrompt-only run; 6 in the full pool). This may be stated as a partial validity check for one source (no under-grouping, over-grouping observed), once saved as a result file; it does not validate grouping on sources without known templates, and the marker-based level assignment is itself heuristic (10 multi-match rows) |
| C2 | On HackAPrompt, recall at 1% FPR with each of 7 lexical groups held out in turn ranged from 0% to 100% (TF-IDF, median 34%) and from 11% to 100% (DeBERTa-v3-small, median 92%), against about 100% under row splits. The groups correspond to the competition's level templates, one group merging about 5 levels, so the held-out unit is a competition level (or merged block of levels) whose wrapper text appears in every row: this measures transfer to unseen level wrappers and level-specific input constraints, not to unseen attack strategies. There are 7 groups and no interval. *Scope narrowed 2026-09-25 by the novelty-auditor from "held-out templates".* | results/leakage/partb/partb_results.json (loto). Lit: hackaprompt-2023 pp. 3–4, 22–25 (level templates), p. 7 §4.4, pp. 24–25 (Level 9 backslash transformation; Level 10 emoji-only), p. 21 (no recommended splits); whenbenchmarkslie-2026 p. 5 §3.3, p. 7 Table 2, p. 8 Table 3 (CV AUC ≥0.996 vs LODO 0.829–0.920); pidsbench-2026 p. 19 §VIII-C ('upper bound'), p. 11/p. 17 Table 13; promptshield-2025 p. 6 §3.1.3 (phrase-disjoint splits); bipia-2025 p. 3 (type-disjoint attack split); injecguard-2024, capture-2025, criticaleval-2026: no held-out-template or row-vs-group evaluation | SUPPORTED-DESCRIPTIVE | PARTIALLY SHOWN. Random splits overstating held-out-source performance is shown at dataset level (WBL pp. 7–8); PIDS-Bench asserts template-derived recall is an upper bound (p. 19) without measuring it. HackAPrompt, InjecGuard, CAPTURE and CriticalEval evaluate no row-vs-held-out-level split (HackAPrompt p. 21; InjecGuard pp. 6–8; CAPTURE p. 2 §2.1; CriticalEval p. 4). Ours: leave-one-level-group-out recall@1% FPR on HackAPrompt with per-level dispersion for two detector families. Skeptic's note: given HackAPrompt pp. 3–4 (a fixed wrapper per level) and WBL, a reader would predict near-100% row-split recall and a held-out drop; only the dispersion is not inferable, and it is partly driven by level-specific input transformations (the 0% TF-IDF group is the slash/Unicode level, HackAPrompt p. 7, pp. 24–25), not template leakage alone. Mapping of Part B pool groups to levels is inferred from sizes and the slashes description, not checked. Frame as a finer-unit illustration of WBL's LODO effect on a known level structure |
| C3 | Design effects for attack recall exceed 1 on all sources: 1.1–2.6 on indirect and jailbreak sources, and 10–400 on HackAPrompt. Example-level intervals are therefore too narrow. | partb_results.json (design_effect); docs/STUDY2_PARTB_RESULTS.md §3. Lit: whenbenchmarkslie-2026 p. 8 (DeLong CIs over 105,034 pooled predictions); pidsbench-2026 p. 7 §IV-D5, p. 16 §VI-H ('example-level bootstrap', 'narrow'); hackaprompt-2023 p. 3 (rows share a level template by construction), p. 21 (duplicates); criticaleval-2026 p. 4 §5.1 (crossed X_c sets, our inference), pp. 4–6 (no intervals); injecguard-2024 all pages, capture-2025 §1–§5 and App. A (no seeds, repeats or intervals); bipia/tasktracker/promptshield/datasentinel/malpid/hierarchical/keyfeature: no intervals | SUPPORTED | NOT FOUND IN 14 PAPERS READ. The method (design effect / cluster variance) is standard statistics; the contribution is the measurement in this domain. The two closest papers use example-level intervals (WBL p. 8; PIDS-Bench p. 7, p. 16); InjecGuard, CAPTURE and CriticalEval report single point estimates. Skeptic's note: the HackAPrompt design effect is expected by construction (6 level groups, HackAPrompt p. 3), rests on 3 repeats with 0–2 groups per test side, and should be stated only as 'well above 1'; the informative part is the modest 1.1–2.6 on sources with small groups |
| C4 | With randomized group splits, row splits did not measurably inflate recall on TaskTracker, BIPIA or jailbreak-classification, for TF-IDF or DeBERTa. All 95% intervals include 0. TF-IDF intervals are about ±10 points; DeBERTa on TaskTracker is [−1.2, +1.5]. | partb_results.json (inflation). Lit: pidsbench-2026 p. 7 §V-A (family separation asserted to eliminate inflation, not measured; cites CheckList [3]); whenbenchmarkslie-2026 p. 7–8 (dataset-level gaps); bipia-2025 p. 3, tasktracker-2025 p. 4 (official splits already source/type-disjoint); capture-2025 p. 2 §2.1 (only seed questions split); injecguard-2024, criticaleval-2026 (no training splits evaluated) | NULL | NOT FOUND IN 14 PAPERS READ. Different unit from When Benchmarks Lie (dataset-level, p. 7): a contrast, not a contradiction. Qualifies PIDS-Bench's unmeasured assumption (p. 7). Skeptic's note: with 1.5–3.7 rows per group, row and group splits differ little by construction, so the null is partly expected; only DeBERTa/TaskTracker bounds inflation tightly |
| C5 | Part A: row splits inflate TaskTracker (+4.2) and BIPIA (+8.0). | docs/STUDY2_RESULTS.md | **WITHDRAWN** (did not replicate) | n/a |
| C6 | ProtectAI v2 recall at 1% FPR: 27.1% [23.4, 30.1] on TaskTracker and 29.8% [19.5, 40.0] on BIPIA, against 85–99% on direct attacks. Ranges are over 5 group splits; no interval; training overlap unknown. | partb_results.json (released). Lit: whenbenchmarkslie-2026 p. 20 Table 13 (8.0% indirect at 4.2% FPR vs 73.6% jailbreak; indirect = BIPIA, InjecAgent, LLMail per Table 12); promptshield-2025 p. 9 Table 4 (TPR 1.97% at 1% FPR, AUC 0.705; threshold fit on eval data, p. 7); injecguard-2024 p. 16 Table 7 (ProtectAIv2 accuracy 8.67% on BIPIA injections vs 88.53% on PINT injections, single run, native decision rule, BIPIA subset unstated); capture-2025 p. 12 Table 6 (the same 8.67%, copied from InjecGuard per p. 4; secondhand), pp. 4 Tables 2–3 (ProtectAIv2 FNR 15–31% on context-aware direct attacks, FPR 27–82%, threshold unstated) | SUPPORTED-DESCRIPTIVE | ALREADY SHOWN (qualitative finding), now by three primary sources: WBL p. 20 Table 13; PromptShield p. 9 Table 4; InjecGuard p. 16 Table 7 (BIPIA 8.67% vs PINT 88.53%, also LakeraGuard 12.00 vs 96.38). CAPTURE p. 12 is not independent evidence (copied from InjecGuard, p. 4); cite InjecGuard, not CAPTURE, for 8.67%. Ours is a replication on different data (TaskTracker, used by none of the four; BIPIA at a fixed 1% FPR with a same-pool direct contrast). Report the magnitude discrepancy (27–30% vs 2–9%) without explaining it; operating points differ (native rule, 4.2% FPR, 1% FPR fit on eval, ours 1% FPR). Keep out of the abstract except as 'consistent with' |
| C7 | With 200 benign target labels, matched retraining did not beat threshold-only (+0.10, 97.5% CI [−0.18, +0.49]) or generic retraining (+0.15, [−0.16, +0.47]). The linear detector had target AUROC 0.648. | results/study/results.json; exploratory_diagnostics.json. Lit: pidsbench-2026 pp. 13–14 §VI-C, Table 10, pp. 15–16 §VI-F, p. 14 and p. 19 §VIII-D ('not tested'); promptshield-2025 p. 7, p. 9 (adjacent); capture-2025 p. 3 §2.4, p. 4 Table 2, p. 12 Table 5 (in-domain attacks plus benign added to generic InjecGuard data, fixed 0.5 threshold, same-generator test; no threshold-only, generic-only, equal-budget or benign-only comparison); injecguard-2024 p. 6 §4.2, p. 8 Table 2, p. 16 Table 9 (MOF generic benign augmentation raises over-defense accuracy, with a malicious-accuracy trade-off; no threshold-only or budget-matched arm) | NULL (confirmatory) | PARTIALLY SHOWN (PIDS-Bench pp. 13–16: generic benign augmentation and threshold/calibration changes do not fix a target-distribution failure). CAPTURE resolved: it does not pre-empt C7 (no threshold-only, generic-only or equal-budget arm; adds labelled attacks as well as benign; threshold fixed at 0.5; capture-2025 pp. 3–4, 12). InjecGuard MOF is adjacent only (generic benign augmentation evaluated on a benchmark generated by the same procedure; p. 6, p. 15). Ours: a preregistered equal-budget comparison including target-matched benign retraining. It does NOT answer PIDS-Bench's open question (p. 19): their target is benign FPR, ours attack recall at 1% FPR, and our source threshold already met target FPR (1.0%). Still UNVERIFIED vs WAInjectBench App. B and EvoShield (no notes) |
| C8 | A 1% threshold set from B < 100 benign labels has expected FPR 1/(B+1). Observed: 2.9% at B = 25. | results/study/RESULTS.md. Lit (adjacent only): promptshield-2025 p. 7, p. 11 Tables 7, 9; criticaleval-2026 p. 3 §4.2.1 (argues for thresholded FPR/FNR over AUC; does not discuss how thresholds are set) | KNOWN (order statistics; practical note) | not claimed as new (standard order-statistics result; no note covers threshold-setting sample size; cite a statistics text). CriticalEval p. 3 may be cited to motivate fixed-operating-point reporting |
| C9 | The InjecGuard training file is clean against its public evaluation files. Its validation set (144 items) consists of evaluation items; arXiv v3 of the paper does not describe a validation set (other versions UNVERIFIED). | docs/BENCHMARK_AUDIT.md. Lit: injecguard-2024 p. 7 §5.1 (training details omit checkpoint selection), all 17 pages (zero hits for "valid"), p. 14 Tables 4–5 (sums equal the 76,735-row release), p. 14 §B.2 and p. 16 Table 9 (author-stated: MOF scale chosen by the headline evaluation average); capture-2025 p. 3 §2.4 (retrains on the InjecGuard data; no overlap audit); promptshield-2025 p. 6 (checks overlap only with its own eval split) | SUPPORTED (exhaustive) | minor note, not a contribution. NOT FOUND IN 14 PAPERS READ. Resolved against arXiv v3: the paper never mentions a validation or checkpoint-selection set (injecguard-2024, all pages; p. 7 §5.1), so the former 'discloses at a high level' clause is not supported and has been removed from our docs. The ACL 2025 PIGuard version and the repository README remain UNVERIFIED. Distinguish two things in the paper: (i) our code-derived finding that train.py's 144-item validation set consists of evaluation items (undisclosed in v3); (ii) the authors' own stated selection of the MOF scale by the evaluation average (p. 14 §B.2), which is theirs, not ours. Do not infer misconduct. CAPTURE inherits the same data (p. 3), our inference |
| C10 | Deterministic group allocation (largest-first) fixed which templates were tested and produced a spurious inflation result (C5); randomizing the allocation removed it. | STUDY2_RESULTS.md vs STUDY2_PARTB_RESULTS.md. Lit: whenbenchmarkslie-2026 p. 5; promptshield-2025 p. 6 §3.1.3; pidsbench-2026 p. 8 §V-C; bipia-2025 p. 3 §4; capture-2025 p. 2 §2.1 (seed-question split, allocation unstated); injecguard-2024 pp. 6, 14 (per-source sampling unstated, no seed) | SUPPORTED (one case) | NOT FOUND IN 14 PAPERS READ. A cautionary case, not a contribution: unstated or single allocations are the norm in the notes (WBL p. 5; PromptShield p. 6; PIDS-Bench p. 8; BIPIA p. 3; CAPTURE p. 2; InjecGuard p. 14), and that allocation can fix which groups are tested is inferable. Report in the replication-failure paragraph |

## Rules

- A claim moves to the paper's abstract only if Status is SUPPORTED or NULL, and Novelty is not ALREADY SHOWN (unless it is framed as a replication).
- WITHDRAWN claims may appear only in the replication-failure paragraph.
- Changing a Status requires a result file; changing a Novelty requires a page-cited literature note.
